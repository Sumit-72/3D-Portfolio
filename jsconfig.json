{
    "compilerOptions": {
      "baseUrl": ".", // Base directory for resolving paths
      "paths": {
        "@/*": ["src/*"] // Alias for the src folder
      }
    },
    "include": ["src/**/*"] // Ensures only the src folder is considered
  }
  